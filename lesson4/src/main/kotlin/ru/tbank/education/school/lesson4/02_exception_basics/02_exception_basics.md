# Домашнее задание 2: Основы исключений

## Задача 1: Иерархия исключений

**Задание**:
Создайте собственную иерархию исключений для системы интернет-магазина:

- Базовое исключение `ShopException`
- 3-4 специфичных исключения (например, `ProductNotFoundException`, `InsufficientStockException`)
- Каждое исключение должно иметь осмысленное сообщение

```kotlin
// Ваш код здесь
sealed class ShopException(message: String) : Exception(message)

// Добавьте свои исключения
```

## Задача 2: Перехват исключений

Дан код:

```kotlin
fun readConfig(filename: String): Config {
    val file = File(filename)
    val content = file.readText()
    return parseConfig(content)
}
```

**Задание**:

- Какие исключения может выбросить этот код?
- Напишите обработчик, который:
    - Ловит ошибки чтения файла
    - Ловит ошибки парсинга
    - Возвращает конфигурацию по умолчанию при любой ошибке
    - Логирует все ошибки

## Задача 3: Множественный catch

Напишите функцию `parseAndCalculate(input: String): Double`, которая:

- Парсит строку в число
- Вычисляет квадратный корень
- Делит результат на 2

**Задание**:

- Обработайте все возможные исключения отдельными catch блоками
- Для каждого типа ошибки верните разное значение по умолчанию
- Добавьте понятные сообщения об ошибках

```kotlin
fun parseAndCalculate(input: String): Double {
    try {
        // Ваш код
    } catch (e: NumberFormatException) {
        // Обработка
    } // Добавьте другие catch блоки
}
```
